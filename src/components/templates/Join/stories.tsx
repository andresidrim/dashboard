import { Meta, StoryObj } from '@storybook/react'
import Join from '.'

export default {
    title: 'Components/Templates/Join',
    component: Join,
} as Meta

type Story = StoryObj

export const Default: Story = {}
