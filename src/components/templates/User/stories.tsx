import { Meta, StoryObj } from '@storybook/react'
import User from '.'

export default {
    title: 'Components/Templates/User',
    component: User,
} as Meta

type Story = StoryObj

export const Default: Story = {}
