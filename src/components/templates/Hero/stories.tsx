import { Meta, StoryObj } from '@storybook/react'
import Hero from '.'

export default {
    title: 'Components/Templates/Hero',
    component: Hero,
} as Meta

type Story = StoryObj

export const Default: Story = {}
